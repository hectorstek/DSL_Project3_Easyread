vllm serve RedHatAI/Qwen2.5-VL-7B-Instruct-quantized.w4a16 \
  --host 0.0.0.0 --port 8000 \
  --trust-remote-code \
  --max-model-len 4096 \
  --limit-mm-per-prompt '{"image": 3, "video": 0}' \
  > vllm.log 2>&1 &

# 4. Wait for it
until curl -s http://localhost:8000/v1/models | grep -q "Qwen2.5-VL"; do echo "Waiting..."; sleep 10; done; echo "Ready"

# 5. Run script
python3 labels_embedding.py